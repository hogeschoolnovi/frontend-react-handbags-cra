import React from "react";




function Button() {
    return (
    <button
        onClick={() => console.log("to the collection")}>
        to the collection
    </button>
    );

}

export default Button;